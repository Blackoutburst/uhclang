# UgValidationLayer

Khronos validation layer list

<H2 id="count">count</H2>

Get the number of required validation layers

<CodeTabs
  uhc={`I32 count();`}
  cpp={`int count();`}
/>

**Returns** — Validation layer count

---

<H2 id="get">get</H2>

Get the array of required validation layer name strings

<CodeTabs
  uhc={`const I8** get();`}
  cpp={`const char** get();`}
/>

**Returns** — Pointer to the array of layer name strings
