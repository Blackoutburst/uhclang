# UgFrameBuffer

Framebuffer array tied to swapchain images

<H2 id="It">It</H2>

Array of framebuffers covering all swapchain images

<CodeTabs
  uhc={`struct It {
    VkFramebuffer* handles;
    VkExtent2D extends;
    U32 bufferCount;
};
  `}
  cpp={`struct It {
    VkFramebuffer* handles;
    VkExtent2D extends;
    unsigned int bufferCount;
};
  `}
/>

---

<H2 id="destroy">destroy</H2>

Destroy all framebuffers in the array

<CodeTabs
  uhc={`U0 destroy(UgContext.It& ctx, UgFrameBuffer.It& framebuffers);`}
  cpp={`void destroy(UgContext::It& ctx, UgFrameBuffer::It& framebuffers);`}
/>

**Parameters**
- `ctx` — Vulkan context
- `framebuffers` — Framebuffer array to destroy

---

<H2 id="create">create</H2>

Create one framebuffer per swapchain image

<CodeTabs
  uhc={`UgFrameBuffer.It create(UgContext.It& ctx, UgSwapchain.It& swapchain, VkRenderPass renderPass);`}
  cpp={`UgFrameBuffer::It create(UgContext::It& ctx, UgSwapchain::It& swapchain, VkRenderPass renderPass);`}
/>

**Parameters**
- `ctx` — Vulkan context
- `swapchain` — Swapchain whose images the framebuffers wrap
- `renderPass` — Render pass the framebuffers are compatible with

**Returns** — Initialized framebuffer array
