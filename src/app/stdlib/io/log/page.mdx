# Log

Leveled logger with colored console output

<H2 id="Level">Level</H2>

Log level values for filtering output

<CodeTabs
  uhc={`enum Level {
    UHC_LOG_TRACE,
    UHC_LOG_DEBUG,
    UHC_LOG_INFO,
    UHC_LOG_WARN,
    UHC_LOG_ERROR,
};
  `}
  cpp={`enum Level {
    UHC_LOG_TRACE,
    UHC_LOG_DEBUG,
    UHC_LOG_INFO,
    UHC_LOG_WARN,
    UHC_LOG_ERROR,
};
  `}
/>
