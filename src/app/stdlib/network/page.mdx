# Network

TCP networking

- [TcpClient](/stdlib/network/tcpclient) — TCP client for connecting to a server
- [TcpServer](/stdlib/network/tcpserver) — TCP server for accepting and managing client connections

<H2 id="Header">Header</H2>

<CodeTabs
  uhc={`#include <uhcnet.uhh>`}
  cpp={`#include <uhcnet.hh>`}
/>

<H2 id="Header">Cheat sheet</H2>

<CodeTabs
  uhc={`namespace TcpClient {
    #if defined(_WIN32) || defined(_WIN64)
      struct It {
        SOCKET sockfd = INVALID_SOCKET
        HANDLE thread = NULL
      }
    #else
      struct It {
        I32 sockfd = -1
        pthread_t thread = NULL
      }
    #endif
}

namespace TcpServer {
    #if defined(_WIN32) || defined(_WIN64)
      struct It {
        SOCKET sockfd = INVALID_SOCKET
        U8 running = 0
        U16 slots = 0
      }
    #else
      struct It {
        I32 sockfd = -1
        U8 running = 0
        U16 slots = 0
      }
    #endif
}
  `}
  cpp={`namespace TcpClient {
    #if defined(_WIN32) || defined(_WIN64)
      struct It {
        SOCKET sockfd = INVALID_SOCKET
        HANDLE thread = NULL
      }
    #else
      struct It {
        int sockfd = -1
        pthread_t thread = NULL
      }
    #endif
}

namespace TcpServer {
    #if defined(_WIN32) || defined(_WIN64)
      struct It {
        SOCKET sockfd = INVALID_SOCKET
        unsigned char running = 0
        unsigned short slots = 0
      }
    #else
      struct It {
        int sockfd = -1
        unsigned char running = 0
        unsigned short slots = 0
      }
    #endif
}
  `}
/>
