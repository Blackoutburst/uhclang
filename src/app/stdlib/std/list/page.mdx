# List

Generic doubly-linked list

<H2 id="It">It</H2>

List container object

<CodeTabs
  uhc={`struct It {
    std::list<T> handle
};
  `}
  cpp={`struct It {
    std::list<T> handle;
};
  `}
/>

---

<H2 id="add">add</H2>

Append a value to the end of the list

<CodeTabs
  uhc={`U0 add(List<T> list, T value);`}
  cpp={`void add(List::It<T>& list, T value);`}
/>

**Parameters**
- `list` — Target list
- `value` — Value to append

---

<H2 id="addAt">addAt</H2>

Insert a value at a specific index

<CodeTabs
  uhc={`U0 addAt(List<T> list, T value, I32 index);`}
  cpp={`void addAt(List::It<T>& list, T value, int index);`}
/>

**Parameters**
- `list` — Target list
- `value` — Value to insert
- `index` — Zero-based position

---

<H2 id="remove">remove</H2>

Remove the first matching value from the list

<CodeTabs
  uhc={`U8 remove(List<T> list, T value);`}
  cpp={`unsigned char remove(List::It<T>& list, T value);`}
/>

**Parameters**
- `list` — Target list
- `value` — Value to find and remove

**Returns** — Non-zero if element was found and removed

---

<H2 id="removeAt">removeAt</H2>

Remove the element at a specific index

<CodeTabs
  uhc={`U0 removeAt(List<T> list, I32 index);`}
  cpp={`void removeAt(List::It<T>& list, int index);`}
/>

**Parameters**
- `list` — Target list
- `index` — Zero-based position to remove

---

<H2 id="get">get</H2>

Get a pointer to the element at a specific index

<CodeTabs
  uhc={`T* get(List<T> list, I32 index);`}
  cpp={`T* get(List::It<T>& list, int index);`}
/>

**Parameters**
- `list` — Target list
- `index` — Zero-based position

**Returns** — Pointer to the element

---

<H2 id="set">set</H2>

Set the value of an element at a specific index

<CodeTabs
  uhc={`U0 set(List<T> list, I32 index, T value);`}
  cpp={`void set(List::It<T>& list, int index, T value);`}
/>

**Parameters**
- `list` — Target list
- `index` — Zero-based position
- `value` — New value

---

<H2 id="first">first</H2>

Get a pointer to the first element

<CodeTabs
  uhc={`T* first(List<T> list);`}
  cpp={`T* first(List::It<T>& list);`}
/>

**Parameters**
- `list` — Target list

**Returns** — Pointer to first element, or NULL if empty

---

<H2 id="last">last</H2>

Get a pointer to the last element

<CodeTabs
  uhc={`T* last(List<T> list);`}
  cpp={`T* last(List::It<T>& list);`}
/>

**Parameters**
- `list` — Target list

**Returns** — Pointer to last element, or NULL if empty

---

<H2 id="size">size</H2>

Get the number of elements in the list

<CodeTabs
  uhc={`U64 size(List<T> list);`}
  cpp={`unsigned long long size(List::It<T>& list);`}
/>

**Parameters**
- `list` — Target list

**Returns** — Element count

---

<H2 id="clear">clear</H2>

Remove all elements from the list

<CodeTabs
  uhc={`U0 clear(List<T> list);`}
  cpp={`void clear(List::It<T>& list);`}
/>

**Parameters**
- `list` — Target list
