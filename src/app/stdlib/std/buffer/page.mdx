# Buffer

Binary buffer with cursor-based reading and writing

<H2 id="It">It</H2>

Buffer object

<CodeTabs
  uhc={`struct It {
    U0* handle
    U8 order
    U32 cursor
};
  `}
  cpp={`struct It {
    void* handle
    unsigned char order
    unsigned int cursor
};
  `}
/>
