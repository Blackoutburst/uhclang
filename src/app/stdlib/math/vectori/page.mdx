# VectorI

Four-component integer vector (x, y, z, w)

<H2 id="It">It</H2>

Integer vector object

<CodeTabs
  uhc={`struct It {
    I32 x
    I32 y
    I32 z
    I32 w
};
  `}
  cpp={`struct It {
    int x;
    int y;
    int z;
    int w;
};
  `}
/>

---

<H2 id="equals">equals</H2>

Test whether two integer vectors are component-wise equal

<CodeTabs
  uhc={`U8 equals(VectorI a, VectorI b)`}
  cpp={`unsigned char equals(VectorI::It& a, VectorI::It& b);`}
/>

**Parameters**
- `a` — First vector
- `b` — Second vector

**Returns** — Non-zero if equal

---

<H2 id="set">set</H2>

Set all four components of an integer vector

<CodeTabs
  uhc={`U0 set(VectorI vec, I32 x, I32 y, I32 z, I32 w)`}
  cpp={`void set(VectorI::It& vec, int x, int y, int z, int w);`}
/>

**Parameters**
- `vec` — Target vector
- `x` — X component
- `y` — Y component
- `z` — Z component
- `w` — W component

---

<H2 id="normalize">normalize</H2>

Normalize an integer vector to unit length in-place

<CodeTabs
  uhc={`U0 normalize(VectorI vec)`}
  cpp={`void normalize(VectorI::It& vec);`}
/>

**Parameters**
- `vec` — Vector to normalize

---

<H2 id="length">length</H2>

Compute the length (magnitude) of an integer vector

<CodeTabs
  uhc={`F32 length(VectorI vec)`}
  cpp={`float length(VectorI::It& vec);`}
/>

**Parameters**
- `vec` — The vector

**Returns** — Euclidean length as float

---

<H2 id="zero">zero</H2>

Set all components of an integer vector to zero

<CodeTabs
  uhc={`U0 zero(VectorI vec)`}
  cpp={`void zero(VectorI::It& vec);`}
/>

**Parameters**
- `vec` — Vector to zero

---

<H2 id="dot">dot</H2>

Compute the dot product of two integer vectors

<CodeTabs
  uhc={`I32 dot(VectorI a, VectorI b)`}
  cpp={`int dot(VectorI::It& a, VectorI::It& b);`}
/>

**Parameters**
- `a` — First vector
- `b` — Second vector

**Returns** — Dot product

---

<H2 id="cross">cross</H2>

Compute the cross product of two integer vectors

<CodeTabs
  uhc={`U0 cross(VectorI result, VectorI a, VectorI b)`}
  cpp={`void cross(VectorI::It& result, VectorI::It& a, VectorI::It& b);`}
/>

**Parameters**
- `result` — Vector to store the result
- `a` — First vector
- `b` — Second vector

---

<H2 id="sub">sub</H2>

Subtract one integer vector from another

<CodeTabs
  uhc={`U0 sub(VectorI result, VectorI a, VectorI b)`}
  cpp={`void sub(VectorI::It& result, VectorI::It& a, VectorI::It& b);`}
/>

**Parameters**
- `result` — Vector to store the result
- `a` — Minuend vector
- `b` — Subtrahend vector
