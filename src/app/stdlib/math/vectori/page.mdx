# VectorI

Four-component integer vector (x, y, z, w)

<H2 id="It">It</H2>

Integer vector object

<CodeTabs
  uhc={`self {
    I32 x
    I32 y
    I32 z
    I32 w
    bool operator==(const It& o) { return x==o.x && y==o.y && z==o.z && w==o.w; }
    It operator+(const It& o) { return {x+o.x, y+o.y, z+o.z, w+o.w}; }
    It operator-(const It& o) { return {x-o.x, y-o.y, z-o.z, w-o.w}; }
    It operator-() { return {-x, -y, -z, -w}; }
    It operator*(const It& o) { return {x*o.x, y*o.y, z*o.z, w*o.w}; }
    It operator*(int s) { return {x*s, y*s, z*s, w*s}; }
    It operator/(int s) { return {x/s, y/s, z/s, w/s}; }
}
  `}
  cpp={`struct It {
    int x;
    int y;
    int z;
    int w;
    bool operator==(const It& o) { return x==o.x && y==o.y && z==o.z && w==o.w; }
    It operator+(const It& o) { return {x+o.x, y+o.y, z+o.z, w+o.w}; }
    It operator-(const It& o) { return {x-o.x, y-o.y, z-o.z, w-o.w}; }
    It operator-() { return {-x, -y, -z, -w}; }
    It operator*(const It& o) { return {x*o.x, y*o.y, z*o.z, w*o.w}; }
    It operator*(int s) { return {x*s, y*s, z*s, w*s}; }
    It operator/(int s) { return {x/s, y/s, z/s, w/s}; }
};
  `}
/>

---

<H2 id="equals">equals</H2>

Test whether two integer vectors are component-wise equal

<CodeTabs
  uhc={`U8 equals(VectorI a, VectorI b)`}
  cpp={`unsigned char equals(VectorI::It& a, VectorI::It& b);`}
/>

**Parameters**
- `a` — First vector
- `b` — Second vector

**Returns** — Non-zero if equal

---

<H2 id="set">set</H2>

Set all four components of an integer vector

<CodeTabs
  uhc={`U0 set(VectorI vec, I32 x, I32 y, I32 z, I32 w)`}
  cpp={`void set(VectorI::It& vec, int x, int y, int z, int w);`}
/>

**Parameters**
- `vec` — Target vector
- `x` — X component
- `y` — Y component
- `z` — Z component
- `w` — W component

---

<H2 id="normalize">normalize</H2>

Normalize an integer vector to unit length in-place

<CodeTabs
  uhc={`U0 normalize(VectorI vec)`}
  cpp={`void normalize(VectorI::It& vec);`}
/>

**Parameters**
- `vec` — Vector to normalize

---

<H2 id="length">length</H2>

Compute the length (magnitude) of an integer vector

<CodeTabs
  uhc={`F32 length(VectorI vec)`}
  cpp={`float length(VectorI::It& vec);`}
/>

**Parameters**
- `vec` — The vector

**Returns** — Euclidean length as float

---

<H2 id="zero">zero</H2>

Set all components of an integer vector to zero

<CodeTabs
  uhc={`U0 zero(VectorI vec)`}
  cpp={`void zero(VectorI::It& vec);`}
/>

**Parameters**
- `vec` — Vector to zero

---

<H2 id="dot">dot</H2>

Compute the dot product of two integer vectors

<CodeTabs
  uhc={`I32 dot(VectorI a, VectorI b)`}
  cpp={`int dot(VectorI::It& a, VectorI::It& b);`}
/>

**Parameters**
- `a` — First vector
- `b` — Second vector

**Returns** — Dot product

---

<H2 id="cross">cross</H2>

Compute the cross product of two integer vectors

<CodeTabs
  uhc={`U0 cross(VectorI result, VectorI a, VectorI b)`}
  cpp={`void cross(VectorI::It& result, VectorI::It& a, VectorI::It& b);`}
/>

**Parameters**
- `result` — Vector to store the result
- `a` — First vector
- `b` — Second vector

---

<H2 id="add">add</H2>

Add two integer vectors component-wise

<CodeTabs
  uhc={`U0 add(VectorI result, VectorI a, VectorI b)`}
  cpp={`void add(VectorI::It& result, VectorI::It& a, VectorI::It& b);`}
/>

**Parameters**
- `result` — Vector to store the result
- `a` — First vector
- `b` — Second vector

---

<H2 id="sub">sub</H2>

Subtract one integer vector from another

<CodeTabs
  uhc={`U0 sub(VectorI result, VectorI a, VectorI b)`}
  cpp={`void sub(VectorI::It& result, VectorI::It& a, VectorI::It& b);`}
/>

**Parameters**
- `result` — Vector to store the result
- `a` — Minuend vector
- `b` — Subtrahend vector

---

<H2 id="mul">mul</H2>

Multiply two integer vectors component-wise

<CodeTabs
  uhc={`U0 mul(VectorI result, VectorI a, VectorI b)`}
  cpp={`void mul(VectorI::It& result, VectorI::It& a, VectorI::It& b);`}
/>

**Parameters**
- `result` — Vector to store the result
- `a` — First vector
- `b` — Second vector

---

<H2 id="scale">scale</H2>

Multiply an integer vector by a scalar

<CodeTabs
  uhc={`U0 scale(VectorI result, VectorI a, I32 s)`}
  cpp={`void scale(VectorI::It& result, VectorI::It& a, int s);`}
/>

**Parameters**
- `result` — Vector to store the result
- `a` — Source vector
- `s` — Scalar factor

---

<H2 id="div">div</H2>

Divide an integer vector by a scalar

<CodeTabs
  uhc={`U0 div(VectorI result, VectorI a, I32 s)`}
  cpp={`void div(VectorI::It& result, VectorI::It& a, int s);`}
/>

**Parameters**
- `result` — Vector to store the result
- `a` — Source vector
- `s` — Scalar divisor
