# Matrix

4x4 row-major float matrix

<H2 id="It">It</H2>

Matrix object (16 floats, initialized to identity)

<CodeTabs
  uhc={`struct It {
    F32 values[16]
};
  `}
  cpp={`struct It {
    float values[16];
};
  `}
/>

---

<H2 id="setIdentity">setIdentity</H2>

Reset a matrix to the identity matrix

<CodeTabs
  uhc={`U0 setIdentity(Matrix mat)`}
  cpp={`void setIdentity(Matrix::It& mat);`}
/>

**Parameters**
- `mat` — The matrix to reset

---

<H2 id="multiply">multiply</H2>

Multiply two matrices and store the result

<CodeTabs
  uhc={`U0 multiply(Matrix result, Matrix left, Matrix right)`}
  cpp={`void multiply(Matrix::It& result, Matrix::It& left, Matrix::It& right);`}
/>

**Parameters**
- `result` — Matrix to store the result
- `left` — First (left) matrix
- `right` — Second (right) matrix

---

<H2 id="copy">copy</H2>

Copy a matrix into another

<CodeTabs
  uhc={`U0 copy(Matrix src, Matrix dest)`}
  cpp={`void copy(Matrix::It& src, Matrix::It& dest);`}
/>

**Parameters**
- `src` — Source matrix
- `dest` — Destination matrix

---

<H2 id="ortho2D">ortho2D</H2>

Build a 2D orthographic projection matrix

<CodeTabs
  uhc={`U0 ortho2D(Matrix mat, F32 left, F32 right, F32 bottom, F32 top, F32 near, F32 far)`}
  cpp={`void ortho2D(Matrix::It& mat, float left, float right, float bottom, float top, float near, float far);`}
/>

**Parameters**
- `mat` — Output matrix
- `left` — Left boundary
- `right` — Right boundary
- `bottom` — Bottom boundary
- `top` — Top boundary
- `near` — Near plane
- `far` — Far plane

---

<H2 id="ortho2DVk">ortho2DVk</H2>

Build a 2D orthographic projection matrix adjusted for Vulkan's coordinate system

<CodeTabs
  uhc={`U0 ortho2DVk(Matrix mat, F32 left, F32 right, F32 bottom, F32 top, F32 near, F32 far)`}
  cpp={`void ortho2DVk(Matrix::It& mat, float left, float right, float bottom, float top, float near, float far);`}
/>

**Parameters**
- `mat` — Output matrix
- `left` — Left boundary
- `right` — Right boundary
- `bottom` — Bottom boundary
- `top` — Top boundary
- `near` — Near plane
- `far` — Far plane

---

<H2 id="projection">projection</H2>

Build a 3D perspective projection matrix

<CodeTabs
  uhc={`U0 projection(Matrix mat, F32 width, F32 height, F32 fov, F32 near, F32 far)`}
  cpp={`void projection(Matrix::It& mat, float width, float height, float fov, float near, float far);`}
/>

**Parameters**
- `mat` — Output matrix
- `width` — Viewport width
- `height` — Viewport height
- `fov` — Field of view angle in radians
- `near` — Near plane distance
- `far` — Far plane distance

---

<H2 id="scale">scale</H2>

Apply a scale transform to a matrix

<CodeTabs
  uhc={`U0 scale(Matrix mat, F32 x, F32 y, F32 z, F32 w)`}
  cpp={`void scale(Matrix::It& mat, float x, float y, float z, float w);`}
/>

**Parameters**
- `mat` — Matrix to modify
- `x` — X scale factor
- `y` — Y scale factor
- `z` — Z scale factor
- `w` — W scale factor

---

<H2 id="translate">translate</H2>

Apply a translation transform to a matrix

<CodeTabs
  uhc={`U0 translate(Matrix mat, F32 x, F32 y, F32 z, F32 w)`}
  cpp={`void translate(Matrix::It& mat, float x, float y, float z, float w);`}
/>

**Parameters**
- `mat` — Matrix to modify
- `x` — X translation
- `y` — Y translation
- `z` — Z translation
- `w` — W translation

---

<H2 id="rotate">rotate</H2>

Apply a rotation transform to a matrix

<CodeTabs
  uhc={`U0 rotate(Matrix mat, F32 angle, F32 x, F32 y, F32 z)`}
  cpp={`void rotate(Matrix::It& mat, float angle, float x, float y, float z);`}
/>

**Parameters**
- `mat` — Matrix to modify
- `angle` — Rotation angle in radians
- `x` — X axis weight
- `y` — Y axis weight
- `z` — Z axis weight

---

<H2 id="lookAt">lookAt</H2>

Build a view matrix from eye position, target, and up vector

<CodeTabs
  uhc={`U0 lookAt(Matrix mat, VectorF eye, VectorF center, VectorF up)`}
  cpp={`void lookAt(Matrix::It& mat, VectorF::It& eye, VectorF::It& center, VectorF::It& up);`}
/>

**Parameters**
- `mat` — Output view matrix
- `eye` — Camera position
- `center` — Camera target point
- `up` — World up direction
