# Math

Math helper and structure (Matrix, Vector)

- [Math](/stdlib/math/math)  — Helpers function
- [Matrix](/stdlib/math/matrix) — 4x4 matrices
- [VectorF](/stdlib/math/vectorf) — Float based vector 4
- [VectorI](/stdlib/math/vectori) — Int base vector 4

<H2 id="Header">Header</H2>

<CodeTabs
  uhc={`#include <uhcmath.uhh>`}
  cpp={`#include <uhcmath.hh>`}
/>

<H2 id="Header">Cheat sheet</H2>

<CodeTabs
  uhc={`#define UHC_PI 3.141592653589793
    
  namespace Math {
    F32 rad(F32 angle);
    I8 sign(I32 value);
    I8 fsign(F32 value);
    F32 fsignf(F32 value);
    I32 clamp(I32 v, I32 min, I32 max);
    I32 fclamp(F32 v, F32 min, F32 max);
    F32 fclampf(F32 v, F32 min, F32 max);
  }
  
  namespace VectorF {
    struct It {
      F32 x;
      F32 y;
      F32 z;
      F32 w;
    };
    
    U8 equals(VectorF.It& a, VectorF.It& b);
    U0 set(VectorF.It& vec, F32 x, F32 y, F32 z, F32 w);
    U0 normalize(VectorF.It& vec);
    F32 length(VectorF.It& vec);
    U0 zero(VectorF.It& vec);
    F32 dot(VectorF.It& a, VectorF.It& b);
    U0 cross(VectorF.It& result, VectorF.It& a, VectorF.It& b);
    U0 sub(VectorF.It& result, VectorF.It& a, VectorF.It& b);
  }
  
  namespace VectorI {
    struct It {
      I32 x;
      I32 y;
      I32 z;
      I32 w;
    };
    
    U8 equals(VectorI.It& a, VectorI.It& b);
    U0 set(VectorI.It& vec, I32 x, I32 y, I32 z, I32 w);
    U0 normalize(VectorI.It& vec);
    F32 length(VectorI.It& vec);
    U0 zero(VectorI.It& vec);
    I32 dot(VectorI.It& a, VectorI.It& b);
    U0 cross(VectorI.It& result, VectorI.It& a, VectorI.It& b);
    U0 sub(VectorI.It& result, VectorI.It& a, VectorI.It& b);
  }
  
  namespace Matrix {
    #define M00 0x00
    #define M01 0x01
    #define M02 0x02
    #define M03 0x03
    #define M10 0x04
    #define M11 0x05
    #define M12 0x06
    #define M13 0x07
    #define M20 0x08
    #define M21 0x09
    #define M22 0x0A
    #define M23 0x0B
    #define M30 0x0C
    #define M31 0x0D
    #define M32 0x0E
    #define M33 0x0F
    
    struct It {
      F32 values[16];
    };
    
    U0 setIdentity(Matrix.It& mat);
    U0 multiply(Matrix.It& result, Matrix.It& left, Matrix.It& right);
    U0 copy(Matrix.It& src, Matrix.It& dest);
    U0 ortho2D(Matrix.It& mat, F32 left, F32 right, F32 bottom, F32 top, F32 near, F32 far);
    U0 ortho2DVk(Matrix.It& mat, F32 left, F32 right, F32 bottom, F32 top, F32 near, F32 far);
    U0 projection(Matrix.It& mat, F32 width, F32 height, F32 fov, F32 near, F32 far);
    U0 scale(Matrix.It& mat, F32 x, F32 y, F32 z, F32 w);
    U0 translate(Matrix.It& mat, F32 x, F32 y, F32 z, F32 w);
    U0 rotate(Matrix.It& mat, F32 angle, F32 x, F32 y, F32 z);
    U0 lookAt(Matrix.It& mat, VectorF.It& eye, VectorF.It& center, VectorF.It& up);
  }
  `}
  cpp={`#define UHC_PI 3.141592653589793
    
  namespace Math {
    float rad(float angle);
    char sign(int32_t value);
    char fsign(float value);
    float fsignf(float value);
    int32_t clamp(int32_t v, int32_t min, int32_t max);
    int32_t fclamp(float v, float min, float max);
    float fclampf(float v, float min, float max);
  }
  
  namespace VectorF {
    struct It {
      float x;
      float y;
      float z;
      float w;
    };
    
    unsigned char equals(VectorF::It& a, VectorF::It& b);
    void set(VectorF::It& vec, float x, float y, float z, float w);
    void normalize(VectorF::It& vec);
    float length(VectorF::It& vec);
    void zero(VectorF::It& vec);
    float dot(VectorF::It& a, VectorF::It& b);
    void cross(VectorF::It& result, VectorF::It& a, VectorF::It& b);
    void sub(VectorF::It& result, VectorF::It& a, VectorF::It& b);
  }
  
  namespace VectorI {
    struct It {
      int32_t x;
      int32_t y;
      int32_t z;
      int32_t w;
    };
    
    unsigned char equals(VectorI::It& a, VectorI::It& b);
    void set(VectorI::It& vec, int32_t x, int32_t y, int32_t z, int32_t w);
    void normalize(VectorI::It& vec);
    float length(VectorI::It& vec);
    void zero(VectorI::It& vec);
    int32_t dot(VectorI::It& a, VectorI::It& b);
    void cross(VectorI::It& result, VectorI::It& a, VectorI::It& b);
    void sub(VectorI::It& result, VectorI::It& a, VectorI::It& b);
  }
  
  namespace Matrix {
    #define M00 0x00
    #define M01 0x01
    #define M02 0x02
    #define M03 0x03
    #define M10 0x04
    #define M11 0x05
    #define M12 0x06
    #define M13 0x07
    #define M20 0x08
    #define M21 0x09
    #define M22 0x0A
    #define M23 0x0B
    #define M30 0x0C
    #define M31 0x0D
    #define M32 0x0E
    #define M33 0x0F
    
    struct It {
      float values[16];
    };
    
    void setIdentity(Matrix::It& mat);
    void multiply(Matrix::It& result, Matrix::It& left, Matrix::It& right);
    void copy(Matrix::It& src, Matrix::It& dest);
    void ortho2D(Matrix::It& mat, float left, float right, float bottom, float top, float near, float far);
    void ortho2DVk(Matrix::It& mat, float left, float right, float bottom, float top, float near, float far);
    void projection(Matrix::It& mat, float width, float height, float fov, float near, float far);
    void scale(Matrix::It& mat, float x, float y, float z, float w);
    void translate(Matrix::It& mat, float x, float y, float z, float w);
    void rotate(Matrix::It& mat, float angle, float x, float y, float z);
    void lookAt(Matrix::It& mat, VectorF::It& eye, VectorF::It& center, VectorF::It& up);
  }
  `}
/>

