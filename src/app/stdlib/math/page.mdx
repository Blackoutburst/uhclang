# Math

Math helper and structure (Matrix, Vector)

- [Math](/stdlib/math/math) — Scalar math helper functions
- [Matrix](/stdlib/math/matrix) — 4x4 row-major float matrix
- [VectorF](/stdlib/math/vectorf) — Four-component float vector (x, y, z, w)
- [VectorI](/stdlib/math/vectori) — Four-component integer vector (x, y, z, w)

<H2 id="Header">Header</H2>

<CodeTabs
  uhc={`#include <uhcmath.uhh>`}
  cpp={`#include <uhcmath.hh>`}
/>

<H2 id="Header">Cheat sheet</H2>

<CodeTabs
  uhc={`#define UHC_PI 3.141592653589793

namespace Math {
    F32 rad(F32 angle)
    I8 sign(I32 value)
    I8 fsign(F32 value)
    F32 fsignf(F32 value)
    I32 clamp(I32 v, I32 min, I32 max)
    I32 fclamp(F32 v, F32 min, F32 max)
    F32 fclampf(F32 v, F32 min, F32 max)
}

namespace VectorF {
    self {
        F32 x
        F32 y
        F32 z
        F32 w
        bool operator{ return x; }
        It operator+(const It& o) { return {x+o.x, y+o.y, z+o.z, w+o.w}; }
        It operator-(const It& o) { return {x-o.x, y-o.y, z-o.z, w-o.w}; }
        It operator-() { return {-x, -y, -z, -w}; }
        It operator*(const It& o) { return {x*o.x, y*o.y, z*o.z, w*o.w}; }
        It operator*(float s) { return {x*s, y*s, z*s, w*s}; }
        It operator/(float s) { return {x/s, y/s, z/s, w/s}; }
    }
    U8 equals(VectorF a, VectorF b)
    U0 set(VectorF vec, F32 x, F32 y, F32 z, F32 w)
    U0 normalize(VectorF vec)
    F32 length(VectorF vec)
    U0 zero(VectorF vec)
    F32 dot(VectorF a, VectorF b)
    U0 cross(VectorF result, VectorF a, VectorF b)
    U0 add(VectorF result, VectorF a, VectorF b)
    U0 sub(VectorF result, VectorF a, VectorF b)
    U0 mul(VectorF result, VectorF a, VectorF b)
    U0 scale(VectorF result, VectorF a, F32 s)
    U0 div(VectorF result, VectorF a, F32 s)
}

namespace VectorI {
    self {
        I32 x
        I32 y
        I32 z
        I32 w
        bool operator{ return x; }
        It operator+(const It& o) { return {x+o.x, y+o.y, z+o.z, w+o.w}; }
        It operator-(const It& o) { return {x-o.x, y-o.y, z-o.z, w-o.w}; }
        It operator-() { return {-x, -y, -z, -w}; }
        It operator*(const It& o) { return {x*o.x, y*o.y, z*o.z, w*o.w}; }
        It operator*(int s) { return {x*s, y*s, z*s, w*s}; }
        It operator/(int s) { return {x/s, y/s, z/s, w/s}; }
    }
    U8 equals(VectorI a, VectorI b)
    U0 set(VectorI vec, I32 x, I32 y, I32 z, I32 w)
    U0 normalize(VectorI vec)
    F32 length(VectorI vec)
    U0 zero(VectorI vec)
    I32 dot(VectorI a, VectorI b)
    U0 cross(VectorI result, VectorI a, VectorI b)
    U0 add(VectorI result, VectorI a, VectorI b)
    U0 sub(VectorI result, VectorI a, VectorI b)
    U0 mul(VectorI result, VectorI a, VectorI b)
    U0 scale(VectorI result, VectorI a, I32 s)
    U0 div(VectorI result, VectorI a, I32 s)
}

namespace Matrix {
    #define M00 0x00
    #define M01 0x01
    #define M02 0x02
    #define M03 0x03
    #define M10 0x04
    #define M11 0x05
    #define M12 0x06
    #define M13 0x07
    #define M20 0x08
    #define M21 0x09
    #define M22 0x0A
    #define M23 0x0B
    #define M30 0x0C
    #define M31 0x0D
    #define M32 0x0E
    #define M33 0x0F
    self {
        F32 values[16]
        bool operator{ for (int i;i<16;i++) if (values[i]!; return true; }
        It operator+(const It& o) { It r; for (int i;i<16;i++) r.values[i]; return r; }
        It operator-(const It& o) { It r; for (int i;i<16;i++) r.values[i]; return r; }
        It operator*(const It& o) { It r; for (int row;row<4;row++) for (int col;col<4;col++) { r.values[row*4+col]; for (int k;k<4;k++) r.values[row*4+col]+; } return r; }
        It operator*(float s) { It r; for (int i;i<16;i++) r.values[i]; return r; }
    }
    U0 setIdentity(Matrix mat)
    U0 multiply(Matrix result, Matrix left, Matrix right)
    U0 copy(Matrix src, Matrix dest)
    U0 ortho2D(Matrix mat, F32 left, F32 right, F32 bottom, F32 top, F32 near, F32 far)
    U0 ortho2DVk(Matrix mat, F32 left, F32 right, F32 bottom, F32 top, F32 near, F32 far)
    U0 projection(Matrix mat, F32 width, F32 height, F32 fov, F32 near, F32 far)
    U0 scale(Matrix mat, F32 x, F32 y, F32 z, F32 w)
    U0 translate(Matrix mat, F32 x, F32 y, F32 z, F32 w)
    U0 rotate(Matrix mat, F32 angle, F32 x, F32 y, F32 z)
    U0 lookAt(Matrix mat, VectorF eye, VectorF center, VectorF up)
    U0 add(Matrix result, Matrix a, Matrix b)
    U0 sub(Matrix result, Matrix a, Matrix b)
    U0 mul(Matrix result, Matrix a, Matrix b)
    U8 inverse(Matrix result, Matrix src)
}
  `}
  cpp={`#define UHC_PI 3.141592653589793

namespace Math {
    float rad(float angle);
    char sign(int value);
    char fsign(float value);
    float fsignf(float value);
    int clamp(int v, int min, int max);
    int fclamp(float v, float min, float max);
    float fclampf(float v, float min, float max);
}

namespace VectorF {
    struct It {
        float x;
        float y;
        float z;
        float w;
        bool operator{ return x; }
        It operator+(const It& o) { return {x+o.x, y+o.y, z+o.z, w+o.w}; }
        It operator-(const It& o) { return {x-o.x, y-o.y, z-o.z, w-o.w}; }
        It operator-() { return {-x, -y, -z, -w}; }
        It operator*(const It& o) { return {x*o.x, y*o.y, z*o.z, w*o.w}; }
        It operator*(float s) { return {x*s, y*s, z*s, w*s}; }
        It operator/(float s) { return {x/s, y/s, z/s, w/s}; }
    };
    unsigned char equals(VectorF::It& a, VectorF::It& b);
    void set(VectorF::It& vec, float x, float y, float z, float w);
    void normalize(VectorF::It& vec);
    float length(VectorF::It& vec);
    void zero(VectorF::It& vec);
    float dot(VectorF::It& a, VectorF::It& b);
    void cross(VectorF::It& result, VectorF::It& a, VectorF::It& b);
    void add(VectorF::It& result, VectorF::It& a, VectorF::It& b);
    void sub(VectorF::It& result, VectorF::It& a, VectorF::It& b);
    void mul(VectorF::It& result, VectorF::It& a, VectorF::It& b);
    void scale(VectorF::It& result, VectorF::It& a, float s);
    void div(VectorF::It& result, VectorF::It& a, float s);
}

namespace VectorI {
    struct It {
        int x;
        int y;
        int z;
        int w;
        bool operator{ return x; }
        It operator+(const It& o) { return {x+o.x, y+o.y, z+o.z, w+o.w}; }
        It operator-(const It& o) { return {x-o.x, y-o.y, z-o.z, w-o.w}; }
        It operator-() { return {-x, -y, -z, -w}; }
        It operator*(const It& o) { return {x*o.x, y*o.y, z*o.z, w*o.w}; }
        It operator*(int s) { return {x*s, y*s, z*s, w*s}; }
        It operator/(int s) { return {x/s, y/s, z/s, w/s}; }
    };
    unsigned char equals(VectorI::It& a, VectorI::It& b);
    void set(VectorI::It& vec, int x, int y, int z, int w);
    void normalize(VectorI::It& vec);
    float length(VectorI::It& vec);
    void zero(VectorI::It& vec);
    int dot(VectorI::It& a, VectorI::It& b);
    void cross(VectorI::It& result, VectorI::It& a, VectorI::It& b);
    void add(VectorI::It& result, VectorI::It& a, VectorI::It& b);
    void sub(VectorI::It& result, VectorI::It& a, VectorI::It& b);
    void mul(VectorI::It& result, VectorI::It& a, VectorI::It& b);
    void scale(VectorI::It& result, VectorI::It& a, int s);
    void div(VectorI::It& result, VectorI::It& a, int s);
}

namespace Matrix {
    #define M00 0x00
    #define M01 0x01
    #define M02 0x02
    #define M03 0x03
    #define M10 0x04
    #define M11 0x05
    #define M12 0x06
    #define M13 0x07
    #define M20 0x08
    #define M21 0x09
    #define M22 0x0A
    #define M23 0x0B
    #define M30 0x0C
    #define M31 0x0D
    #define M32 0x0E
    #define M33 0x0F
    struct It {
        float values[16];
        bool operator{ for (int i;i<16;i++) if (values[i]!; return true; }
        It operator+(const It& o) { It r; for (int i;i<16;i++) r.values[i]; return r; }
        It operator-(const It& o) { It r; for (int i;i<16;i++) r.values[i]; return r; }
        It operator*(const It& o) { It r; for (int row;row<4;row++) for (int col;col<4;col++) { r.values[row*4+col]; for (int k;k<4;k++) r.values[row*4+col]+; } return r; }
        It operator*(float s) { It r; for (int i;i<16;i++) r.values[i]; return r; }
    };
    void setIdentity(Matrix::It& mat);
    void multiply(Matrix::It& result, Matrix::It& left, Matrix::It& right);
    void copy(Matrix::It& src, Matrix::It& dest);
    void ortho2D(Matrix::It& mat, float left, float right, float bottom, float top, float near, float far);
    void ortho2DVk(Matrix::It& mat, float left, float right, float bottom, float top, float near, float far);
    void projection(Matrix::It& mat, float width, float height, float fov, float near, float far);
    void scale(Matrix::It& mat, float x, float y, float z, float w);
    void translate(Matrix::It& mat, float x, float y, float z, float w);
    void rotate(Matrix::It& mat, float angle, float x, float y, float z);
    void lookAt(Matrix::It& mat, VectorF::It& eye, VectorF::It& center, VectorF::It& up);
    void add(Matrix::It& result, Matrix::It& a, Matrix::It& b);
    void sub(Matrix::It& result, Matrix::It& a, Matrix::It& b);
    void mul(Matrix::It& result, Matrix::It& a, Matrix::It& b);
    unsigned char inverse(Matrix::It& result, Matrix::It& src);
}
  `}
/>
